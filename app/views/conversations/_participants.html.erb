<% conversation.participants.each do |participant| %>
  <% unless participant == current_user %>
    <%= link_to participant.full_name, profile_path(User.find(participant.id)) %>
  <% end %>
<% end %>
